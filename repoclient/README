repoclient:

This package is designed to be installed on the VM to interface with the repoman server. 

To begin, run setup.py.  This will place a repoclient.conf in /etc/repoclient/, where the default repository and userkeys, etc. are set.
